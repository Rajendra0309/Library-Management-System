server {
    listen 80;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        
        # This fixes the 404 error by pointing all unknown routes back to index.html
        # allowing React Router to handle the ProtectedRoutes.
        try_files $uri $uri/ /index.html;
    }
}
